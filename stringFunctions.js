// Return a given string as all upper case
// e.g. - IT WILL LOOK LIKE YOU ARE YELLING
function allCaps (str) {}

// Return a given string as all lower case
// e.g. - it will look like you are too lazy to press the shift key
function noCaps (str) {}

// Return a given string with only the first letter of each word in uppercase
// e.g. - It Will Look Like A Book Title
function firstLetterCaps (str) {}

// Replace a word in a given string with another word
// e.g. - Replace expired with fresh
// The milk is expired.
function wordReplacement (str, find, replacement) {}

// Return the word count of a given string
// e.g. - "How many words is this" should return 5
function wordCount (str) {}

module.exports = {allCaps, noCaps, firstLetterCaps, wordReplacement, wordCount};